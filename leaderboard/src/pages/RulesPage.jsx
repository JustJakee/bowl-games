import PlaceholderPage from "./PlaceholderPage";

const RulesPage = () => {
  return (
    <PlaceholderPage
      title="Rules"
      description="Rules and scoring content is not being rewritten in this pass. This route preserves the new shell and will be expanded later."
    />
  );
};

export default RulesPage;
